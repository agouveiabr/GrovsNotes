---
phase: 01-advanced-parsing-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, convex/schema.ts, convex/lib/parser.ts]
autonomous: true
requirements: [PARSER-01, PARSER-02, PARSER-03, PARSER-04]

must_haves:
  truths:
    - "MultiEntityParser correctly extracts #tags, ^projects, and !!priorities from strings"
    - "chrono-node translates natural language dates into valid Unix timestamps"
    - "Conventional prefixes (feat:, bug:, etc.) are correctly identified"
    - "Clean titles are generated by stripping metadata tokens from the input"
  artifacts:
    - path: "convex/lib/parser.ts"
      provides: "Centralized parsing logic for all metadata tokens"
    - path: "convex/schema.ts"
      contains: "originalInput"
  key_links:
    - from: "convex/lib/parser.ts"
      to: "chrono-node"
      via: "import"
---

<objective>
Implement the core MultiEntityParser and update the database schema to persist all extracted metadata. This provides the foundation for frictionless capture by translating raw text into structured task data.

Purpose: Enable automated metadata extraction from natural language input.
Output: A shared parser utility and updated items schema.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/phase-1/RESEARCH.md
@convex/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies and update schema</name>
  <files>package.json, convex/schema.ts</files>
  <action>
    - Add "chrono-node": "^2.7.7" to the root package.json dependencies (per research).
    - Add "tsx": "^4.7.1" and "vitest": "^3.0.0" to the root package.json devDependencies (to support TS execution and unit testing).
    - Update the "items" table in convex/schema.ts to include:
      - Update "type" field to include 'feat', 'fix', and 'chore' in the union (e.g., v.union(v.literal("idea"), v.literal("task"), v.literal("note"), v.literal("bug"), v.literal("research"), v.literal("feat"), v.literal("fix"), v.literal("chore")))
      - originalInput: v.string() (to store the raw user input per PARSER-04)
      - priority: v.optional(v.number()) (to store !!priority values 1-4)
    - Ensure all existing optional fields (type, status, projectId, dueAt) are correctly defined.
  </action>
  <verify>
    <automated>pnpm install && npx convex dev --once</automated>
  </verify>
  <done>Dependencies installed (including tsx/vitest) and Convex schema updated with new types and persistence fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MultiEntityParser</name>
  <files>convex/lib/parser.ts</files>
  <behavior>
    - Input: "feat: ^grovsnotes #work #ui Fix header !!1 tomorrow"
    - Output: {
        cleanTitle: "Fix header",
        tags: ["work", "ui"],
        project: "grovsnotes",
        priority: 1,
        type: "feat",
        dueAt: [timestamp for tomorrow]
      }
    - Handles missing entities gracefully (returning undefined/empty arrays).
    - Strips all recognized tokens from the cleanTitle.
  </behavior>
  <action>
    Create a robust parser using Regex for tokens and chrono-node for dates.
    Patterns:
    - Tags: #(\w+)
    - Projects: \^(\w+)
    - Priority: !!(1|2|3|4)
    - Prefix: ^(feat|fix|chore|idea|bug|research):\s*
    Date extraction should use chrono.parseDate and return a Unix timestamp (ms).
  </action>
  <verify>
    <automated>npx tsx -e "import { parseItem } from './convex/lib/parser.ts'; console.log(parseItem('test #tag !!1 tomorrow'))"</automated>
  </verify>
  <done>MultiEntityParser implemented and verified against all token patterns using tsx.</done>
</task>

</tasks>

<verification>
Verify that pnpm install succeeds and the parser logic correctly handles combined inputs with all 5 entity types (tags, projects, priorities, prefixes, dates).
</verification>

<success_criteria>
- Schema includes originalInput and priority, and supports feat/fix/chore types.
- MultiEntityParser extracts all 5 metadata types correctly.
- Clean title excludes all tokens and prefix.
</success_criteria>

<output>
After completion, create .planning/phases/phase-1/phase-1-01-SUMMARY.md
</output>
